;; Q: In passing, Ben also cryptically comments: "By testing the signs of the
;; endpoints of the intervals, it is possible to break mul-interval into nine
;; cases, only one of which requires more than two multiplications." Rewrite
;; this procedure using Ben's suggestion.

;; R (for retort): Ben seriously needs to fuck off and this suggestion has no
;; place in a mature computer programmer's thought process :~)
